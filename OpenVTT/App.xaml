<?xml version="1.0" encoding="utf-8"?>
<Application x:Class="OpenVTT.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">
  <Application.Resources>
    <!-- Colors: parchment + deep red accent -->
    <SolidColorBrush x:Key="Parchment" Color="#F6E7C1"/>
    <SolidColorBrush x:Key="Ink" Color="#2B1E11"/>
    <SolidColorBrush x:Key="Accent" Color="#7A1B1D"/>
    <SolidColorBrush x:Key="AccentDark" Color="#3D0F10"/>

    <Style TargetType="Window">
      <Setter Property="Background" Value="{StaticResource Parchment}"/>
      <Setter Property="Foreground" Value="{StaticResource Ink}"/>
      <Setter Property="FontFamily" Value="Segoe UI"/>
    </Style>

    <Style TargetType="Button">
      <Setter Property="Margin" Value="4"/>
      <Setter Property="Padding" Value="10,6"/>
      <Setter Property="Background" Value="{StaticResource Accent}"/>
      <Setter Property="Foreground" Value="White"/>
      <Setter Property="BorderBrush" Value="{StaticResource AccentDark}"/>
    </Style>

    <Style TargetType="ToggleButton" BasedOn="{StaticResource {x:Type Button}}"/>

    <Style TargetType="TabControl">
      <Setter Property="Padding" Value="6"/>
    </Style>

    <Style TargetType="TextBlock">
      <Setter Property="TextWrapping" Value="Wrap"/>
    </Style>
  </Application.Resources>
</Application>
